# Copy to .env and fill in. All vars are optional overrides of config.py defaults.
# The values below mirror the platform defaults; uncomment to override.

# TODO(you): your 12-digit AWS account id (needed to build ECR/role ARNs locally)
# NTD_ACCOUNT_ID=123456789012

# NTD_REGION=us-east-2
# NTD_MASTER_BUCKET=nateeatsrice-master-s3
# NTD_GLUE_GOLD_DATABASE=data_pipeline_gold_dev

# Point the Streamlit UI at a remote API (defaults to http://localhost:8000)
# NTD_API_URL=http://localhost:8000
